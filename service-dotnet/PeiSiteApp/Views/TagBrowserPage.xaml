<UserControl x:Class="PeiSiteApp.Views.TagBrowserPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PeiSiteApp.ViewModels">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="TagRow" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#F1F5F9" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="16,10" />
        </Style>

        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#E2E8F0" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel>

        <!-- Header bar -->
        <Border DockPanel.Dock="Top" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                Padding="24,12">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="PLC Tag Browser" FontSize="18" FontWeight="SemiBold" Foreground="#1E293B" />
                    <TextBlock FontSize="12" Foreground="#64748B" Margin="0,3,0,0">
                        <Run Text="{Binding Tags.Count, Mode=OneWay}" />
                        <Run Text=" tags" />
                    </TextBlock>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <Button Command="{Binding RefreshCommand}" Style="{StaticResource ActionBtn}" Margin="0,0,8,0">
                        <TextBlock Text="Browse PLC" FontSize="12" Foreground="#334155" />
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Filter bar -->
        <Border DockPanel.Dock="Top" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                Padding="24,8">
            <DockPanel>
                <Button DockPanel.Dock="Right" Command="{Binding ClearFilterCommand}"
                        Style="{StaticResource ActionBtn}" Margin="8,0,0,0"
                        Visibility="{Binding FilterText, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="Clear" FontSize="12" Foreground="#64748B" />
                </Button>
                <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4"
                        Background="White" Padding="8,4">
                    <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent"
                             Foreground="#1E293B" FontSize="13"
                             VerticalAlignment="Center"
                             x:Name="FilterBox">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Filter tags..." Foreground="#94A3B8"
                                                                   FontSize="13" Margin="0" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Border>
            </DockPanel>
        </Border>

        <!-- Loading indicator -->
        <Border DockPanel.Dock="Top" Padding="24,8"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Loading..." FontSize="13" Foreground="#64748B" />
        </Border>

        <!-- Status message -->
        <Border DockPanel.Dock="Top" Padding="24,8"
                Visibility="{Binding StatusMessage, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding StatusMessage}" FontSize="13" Foreground="#64748B" />
        </Border>

        <!-- Tag list -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Tags}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource TagRow}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="140" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="13"
                                           Foreground="#1E293B" VerticalAlignment="Center" />

                                <Border Grid.Column="1" Background="#F1F5F9" CornerRadius="4"
                                        Padding="6,2" VerticalAlignment="Center" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding DataType}" FontSize="11" Foreground="#64748B" />
                                </Border>

                                <TextBlock Grid.Column="2" Text="{Binding Scope}" FontSize="12"
                                           Foreground="#94A3B8" VerticalAlignment="Center"
                                           HorizontalAlignment="Right" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
